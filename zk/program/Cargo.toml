[package]
name = "rite-program"
version = "0.1.0"
edition = "2021"

# standalone: built by the SP1 toolchain, not by the host workspace
[workspace]

[dependencies]
sp1-zkvm = "6.3.1"
serde = { version = "1.0", features = ["derive"] }
rite-lib = { path = "../lib" }
